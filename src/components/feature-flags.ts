/**
 * Feature flags. Code for these features stays in the repo but is hidden from
 * users until flipped back on. Set to `true` to re-enable.
 */

// Instagram + Pinterest / Nail Inspo sections (public site + admin card).
export const SHOW_SOCIAL = false;

// Deposits / online payment (deposit chips, Stripe checkout at booking).
export const SHOW_PAYMENTS = false;
